"""doc-query (docq): local, index-and-search retrieval over office documents.

Supported formats: PDF (`extractor_pdf`), pptx / xlsx (`extractor_office`).
The generic core (store/search/tokenize/tokens) is format-agnostic.
"""
